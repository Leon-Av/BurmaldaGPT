# Минимальные версии (>=) вместо жёсткой фиксации (==).
# Это позволяет pip выбрать свежие сборки с готовыми wheel'ами под новые Python
# (включая 3.14), вместо попытки собирать pydantic-core из исходников на Rust.
fastapi>=0.115
uvicorn>=0.34
pydantic>=2.10
